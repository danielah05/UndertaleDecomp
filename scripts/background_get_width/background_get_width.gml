/// @func	background_get_height(index)
/// @desc	(GM Legacy) Finds the width of a background.
/// @param  {GMSprite}	index	The index of the background to find the width of.
/// @returns {real}
function background_get_width()
{
	return sprite_get_width(argument0);
}
